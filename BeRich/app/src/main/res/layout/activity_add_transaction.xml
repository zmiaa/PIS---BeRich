<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <TextView
        android:id="@+id/textTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BeRich"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <GridLayout
        android:id="@+id/gridCategories"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:alignmentMode="alignMargins"
        android:columnCount="3"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/textTitle"
        tools:layout_editor_absoluteX="0dp">

        <Button
            android:id="@+id/catFood"
            style="?attr/materialIconButtonStyle"
            android:text="Food" />

        <Button
            android:id="@+id/catTransport"
            style="?attr/materialIconButtonStyle"
            android:text="Transport" />

        <Button
            android:id="@+id/catShopping"
            style="?attr/materialIconButtonStyle"
            android:text="Shopping" />

        <Button
            android:id="@+id/catSalary"
            style="?attr/materialIconButtonStyle"
            android:text="Salary" />

        <Button
            android:id="@+id/catGift"
            style="?attr/materialIconButtonStyle"
            android:text="Gift" />

        <Button
            android:id="@+id/catBills"
            style="?attr/materialIconButtonStyle"
            android:text="Bills" />
    </GridLayout>

    <TextView
        android:id="@+id/txtAmountDisplay"
        android:layout_width="233dp"
        android:layout_height="52dp"
        android:layout_marginHorizontal="32dp"
        android:layout_marginTop="268dp"
        android:background="@android:drawable/edit_text"
        android:gravity="end"
        android:padding="8dp"
        android:text="0"
        android:textSize="32sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/gridCategories" />

    <GridLayout
        android:id="@+id/numericKeyboard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="4"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            style="?attr/materialButtonOutlinedStyle"
            android:layout_row="0"
            android:layout_column="0"
            android:text="1" />

        <Button android:text="2" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:text="3" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:id="@+id/btnPlus" android:text="+" android:backgroundTint="#D1E9FF"/>

        <Button android:text="4" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:text="5" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:text="6" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:id="@+id/btnMinus" android:text="-" android:backgroundTint="#FFE0B2"/>

        <Button android:text="7" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:text="8" style="?attr/materialButtonOutlinedStyle"/>
        <Button android:text="9" style="?attr/materialButtonOutlinedStyle"/>

        <Button android:layout_columnSpan="3" android:text="0" style="?attr/materialButtonOutlinedStyle"/>

    </GridLayout>

</androidx.constraintlayout.widget.ConstraintLayout>